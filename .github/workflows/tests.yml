name: tests

on: [push, pull_request]

jobs:
  bats:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install bats and envsubst
        run: sudo apt-get update && sudo apt-get install -y bats gettext-base
      - name: Run the test suite
        run: bats tests/
