-- ============================================================
--  GENERATED BY hypr-themes — DO NOT EDIT BY HAND.
--  Regenerated on every `theme <name>`. Edit the .theme file instead.
--  Your config.lua reads these values via require("modules/theme").
-- ============================================================

return {
    border_1     = "${BORDER_COLOR_1}",
    border_2     = "${BORDER_COLOR_2}",
    border_angle = ${BORDER_ANGLE},
    glow         = "${GLOW_COLOR}",
}
