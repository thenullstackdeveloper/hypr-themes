-- ============================================================
--  CONFIG (general, input, decoration, dwindle, misc + animations master)
-- ============================================================

hl.config({
    general = {
        gaps_in = 8,
        gaps_out = 16,
        border_size = 3,
        col = {
            active_border = { colors = { "${BORDER_COLOR_1}", "${BORDER_COLOR_2}" }, angle = ${BORDER_ANGLE} },
            inactive_border = "rgba(595959aa)",
        },
        layout = "dwindle",
        resize_on_border = true,
        allow_tearing = false,
    },

    input = {
        -- IMPORTANTE: Corne V4 requiere kb_layout = "us".
        -- Cambiarlo rompe el mapping de simbolos/numeros/capas del firmware.
        kb_layout = "us",
        follow_mouse = 1,
        sensitivity = 0,
        touchpad = { natural_scroll = true },
    },

    decoration = {
        rounding = 12,
        active_opacity = 1.0,
        inactive_opacity = 0.95,
        blur = {
            enabled = true,
            size = 8,
            passes = 3,
            new_optimizations = true,
            ignore_opacity = true,
        },
        shadow = {
            enabled = true,
            range = 30,
            render_power = 3,
            color = "rgba(1a1a1aee)",
        },
        -- Glow (Hyprland 0.55): tinta de resplandor alrededor de ventana activa
        glow = {
            enabled = true,
            color = "${GLOW_COLOR}",   -- accent con alpha 0x80 (50%)
        },
    },

    dwindle = {
        preserve_split = true,
    },

    misc = {
        disable_hyprland_logo = false,
        disable_splash_rendering = false,
        mouse_move_enables_dpms = true,
        key_press_enables_dpms = true,
        enable_swallow = false,
        focus_on_activate = true,
    },

    -- Master toggle de animaciones. Curves y per-event van en animations.lua
    animations = { enabled = true },
})
