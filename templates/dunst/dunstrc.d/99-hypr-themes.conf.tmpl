# ============================================================
#  GENERATED BY hypr-themes — DO NOT EDIT BY HAND.
#  Regenerated on every `theme <name>`. Edit the .theme file instead.
#  dunst drop-in: loaded after your dunstrc and overrides only the
#  themed colors, so your dunstrc keeps every other setting.
# ============================================================

[global]
    frame_color = "${ACCENT_PRIMARY}"

[urgency_low]
    background = "${BG_BASE_HEX}"
    foreground = "${FG_SUBTEXT0_HEX}"
    frame_color = "${COLOR_OVERLAY_HEX}"

[urgency_normal]
    background = "${BG_BASE_HEX}"
    foreground = "${FG_TEXT_HEX}"
    frame_color = "${ACCENT_PRIMARY}"

[urgency_critical]
    background = "${BG_BASE_HEX}"
    foreground = "${FG_TEXT_HEX}"
    frame_color = "${COLOR_RED_HEX}"
